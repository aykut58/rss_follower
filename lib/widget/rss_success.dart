import 'package:flutter/material.dart';

class RssSuccess extends StatelessWidget {
  RssSuccess ({Key? key,required this.id,}) : super(key: key);
int id;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}